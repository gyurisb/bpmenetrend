<UserControl
    x:Class="CityTransitApp.CityTransitElements.BaseElements.DateTimePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CityTransitApp.CityTransitElements.BaseElements"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="400">

    <Border x:Name="Root" Grid.Column="1" Background="{StaticResource AppFieldBrush}" BorderThickness="0,0,0,2" BorderBrush="{StaticResource AppHeaderBackgroundBrush}" Tapped="DateTimePicker_Tapped">
        <StackPanel Margin="10,10,0,10" Orientation="Horizontal">
            <TextBlock x:Name="TypeText" Text="Indulás:" Foreground="{StaticResource AppFieldForegroundBrush2}" VerticalAlignment="Center" FontSize="20" Margin="0,0,4,0"/>
            <TextBlock x:Name="DateTimeText" Text="10:00" Foreground="{StaticResource AppFieldForegroundBrush}" VerticalAlignment="Center" FontSize="20"/>
        </StackPanel>
    </Border>
</UserControl>
